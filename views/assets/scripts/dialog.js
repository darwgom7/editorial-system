{
	/*const btnTest = document.getElementById('btnTestDialog');
	const modalCurtain = document.getElementById('modalCurtain');
	const modal = document.getElementById('modal');
	const info = document.getElementById('info');
	btnTest !== null &&
		btnTest.addEventListener('click', e => {
			modalCurtain.classList.remove('modal__curtain--hide');
			modalCurtain.classList.add('modal__curtain--show');
			modal.classList.remove('modal__hide');
			modal.classList.add('modal__show');
		});
	modal !== null &&
		modal.addEventListener('click', e => {
			if (e.target.id === 'modalBtnOk') {
				info.innerText = 'Yuju...😊';
				console.log('ok');
			} else if (e.target.id === 'modalBtnKo') {
				info.innerText = 'Oh no...😏';
				modalCurtain.classList.remove('modal__curtain--show');
				modalCurtain.classList.add('modal__curtain--hide');
				modal.classList.remove('modal__show');
				modal.classList.add('modal__hide');
			} else if (e.target.id === 'modalBtnClose') {
				info.innerText = 'Eyyy...😣';
				modalCurtain.classList.remove('modal__curtain--show');
				modalCurtain.classList.add('modal__curtain--hide');
				modal.classList.remove('modal__show');
				modal.classList.add('modal__hide');
			}
			console.log('event', e);
		});
		*/
}
